apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: <generated>
  tags:
    environment: integration-test

# Only Karpenter configuration - no karpenter.sh/discovery in metadata tags
karpenter:
  version: 'v1.6.2'

iam:
  withOIDC: true

managedNodeGroups:
  - name: managed-ng-1
    minSize: 1
    maxSize: 2
    desiredCapacity: 1