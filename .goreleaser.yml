before:
  hooks:
  - make install-bindata
  - make update-bindata
builds:
  - main: ./cmd/eksctl
    binary: eksctl
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
archive:
  format: tar.gz
  files:
  - none*
