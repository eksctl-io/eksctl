apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: <generated>

vpc:
  hostnameType: resource-name

managedNodeGroups:
  - name: mng-1
    desiredCapacity: 2
    maxSize: 2
    minSize: 1
