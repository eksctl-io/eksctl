kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: oidc-reader
rules:
  - apiGroups:
      - ""
    resources:
      - "*"
    verbs:
      - "get"
      - "watch"
      - "list"
