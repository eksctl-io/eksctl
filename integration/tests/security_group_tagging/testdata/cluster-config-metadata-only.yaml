apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: <generated>
  tags:
    karpenter.sh/discovery: <generated>
    environment: integration-test

# No Karpenter configuration - only metadata tags

iam:
  withOIDC: true

managedNodeGroups:
  - name: managed-ng-1
    minSize: 1
    maxSize: 2
    desiredCapacity: 1