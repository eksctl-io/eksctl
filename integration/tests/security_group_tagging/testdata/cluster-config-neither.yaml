apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: <generated>
  tags:
    environment: integration-test

# Neither Karpenter configuration nor karpenter.sh/discovery in metadata tags

iam:
  withOIDC: true

managedNodeGroups:
  - name: managed-ng-1
    minSize: 1
    maxSize: 2
    desiredCapacity: 1