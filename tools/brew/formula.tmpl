# This file was generated by eksctl/tools/brew/brewtap.go. DO NOT EDIT.
class Eksctl < Formula
  desc "The official CLI for Amazon EKS"
  homepage "https://eksctl.io/"
  version "{{ .Version }}"

  if OS.mac?
    url "{{ .Mac.URL }}"
    sha256 "{{ .Mac.Checksum }}"
  elsif OS.linux?
    url "{{ .Linux.URL }}"
    sha256 "{{ .Linux.Checksum}}"
  end

  depends_on "kubernetes-cli"
  depends_on "aws-iam-authenticator"

  def install
    bin.install "eksctl"
  end
end
